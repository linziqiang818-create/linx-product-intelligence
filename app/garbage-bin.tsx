type GarbageProduct = { asin:string; title:string; titleZh?:string; category:string; price:number; sourceUrl:string };

export default function GarbageBin({products,restore,empty}:{products:GarbageProduct[];restore:(product:GarbageProduct)=>void;empty:()=>void}){return <section className="panel rejected-panel"><div className="section-title"><div><span className="eyebrow">AUTO REJECTED</span><h2>垃圾箱</h2><p>抓取后确认触发硬性禁做条件的产品会自动移到这里，不参与排名、统计、校准或收藏。数据缺失和方向暂不匹配不会进入垃圾箱。</p></div><div className="actions"><span className="tag">{products.length} 个已淘汰</span><button className="danger" disabled={!products.length} onClick={empty}>彻底清空</button></div></div><div className="table-wrap"><table><thead><tr><th>ASIN / 商品</th><th>类目</th><th>价格</th><th>淘汰方式</th><th>操作</th></tr></thead><tbody>{products.map(p=><tr key={p.asin}><td><div className="table-product"><div><b>{p.asin}</b><span><a href={p.sourceUrl} target="_blank" rel="noreferrer">{p.title}</a><small className="title-zh">{p.titleZh||"中文完整标题待自动翻译"}</small></span></div></div></td><td>{p.category}</td><td>${p.price}</td><td><span className="risk risk-高">硬性条件</span></td><td><button className="link" onClick={()=>restore(p)}>恢复并编辑</button></td></tr>)}</tbody></table>{!products.length&&<div className="empty table-empty"><b>垃圾箱为空</b><span>只有确认不符合公司硬性条件的产品才会自动进入这里。</span></div>}</div></section>}
